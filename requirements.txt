# เวอร์ชันตายตัว (ไม่ใช่ >=) — ตั้งใจ ล็อกตามที่ทดสอบผ่านจริงในเครื่องแล้วทั้งหมด
# เพื่อให้ pip resolve เร็วและได้ผลลัพธ์เดิมทุกครั้งตอน deploy (เช่น Streamlit Cloud)
# ไม่ต้องมานั่งเดา/backtrack หาชุดเวอร์ชันที่เข้ากันได้เอง

# ELT stage
duckdb==1.5.5
huggingface_hub==1.26.0
PyYAML==6.0.3
python-dotenv==1.2.2   # อ่าน HF_TOKEN จากไฟล์ .env

# Preprocessing stage (notebook)
pandas==2.3.3
pyarrow==24.0.0
numpy==2.2.6
ipykernel==7.3.0
nbformat==5.10.4
nbclient==0.11.0

# Analytics + model notebooks
matplotlib==3.10.9
scikit-learn==1.7.2
joblib==1.5.3

# Dashboard (dashboard/app.py)
streamlit==1.61.1
plotly==6.9.0

# ไม่บังคับ — สำหรับเปิด notebook แบบ interactive
# jupyterlab
